We want to refurbish the webapp here-

We will have every segment in the UI -

1) We will have every thing as a page - and then a sidebar to toggle through the pages as well.

Page for login - We need to put the PSID -

And then once done - we go to the next page - to upload all the datasets.

We then show the details of the missing things if found -

1) Show all the tables - input which are required and show tick or wrong if found or not.

2) Show if all the tables have the same columns - except for tables - which are Date columns. For example - in Other tables - we need to make sure all the tables have the same schema as before - the columns can be ordered differently but the schema should be same. If Country is the column - it has to be there. If not Found - Mention the table name and the column name - as not found.

3) Once everything is right - then we go for the run.

4) Then we show the Assessment Output finally. We show the download output button there to download the output.

5) Should be the page - where the user can click to add the Final Inherent Credit Risk Assessment for every Assessment output - Product and Country.

Make sure you make it user friendly and modern Light UI.
